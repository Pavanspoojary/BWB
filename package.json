{
  "name": "toolarena",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "description": "TOOL//ARENA — a live dev-tool deathmatch directory. Vite + TypeScript, Supabase realtime.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview --port 8000",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.4"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "typescript": "^5.7.3",
    "vite": "^6.0.7",
    "vitest": "^2.1.8"
  }
}
